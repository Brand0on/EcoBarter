<div>
  {{#if image}}
    <img src="{{image}}" alt="description" />
  {{/if}}
  <p>{{description}}</p>
  <small>{{createdAt}}
    - by
    <a href="/profile/{{author._id}}">{{author.name}}</a></small>
</div>